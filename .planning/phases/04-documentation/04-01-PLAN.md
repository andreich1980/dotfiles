---
phase: 04-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [README.md]
autonomous: true
requirements: [DOC-01, DOC-02]

must_haves:
  truths:
    - "README.md exists in the root directory"
    - "README contains manual installation commands for Linux Mint (apt)"
    - "README reflects the actual CLI output of the install script (emojis, indentation)"
    - "README explains the local override pattern (config.local.fish)"
    - "README includes Node.js path handling examples in the overrides section"
  artifacts:
    - path: "README.md"
      provides: "User guidance and tool provisioning instructions"
  key_links:
    - from: "README.md"
      to: "./install"
      via: "Usage instructions"
---

<objective>
Deliver comprehensive project documentation that bridges the gap between the automated configuration engine and the manual tool provisioning process.

Purpose: Provide a "Zero to Hero" guide for Linux Mint users.
Output: README.md
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-documentation/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate comprehensive README.md</name>
  <files>README.md</files>
  <action>
    Create a new README.md based on the architecture patterns in 04-RESEARCH.md.
    
    Ensure the following sections are included (per D-XX and Research):
    - **Project Name & Description**: Dotfiles Management Improvement.
    - **Features**: Modular, Idempotent, Atomic, Dry-run support.
    - **Prerequisites**: Explicit `sudo apt install -y git vim fish curl xdg-utils speedtest-cli build-essential` commands.
    - **Installation**: `git clone`, `./install`, and `./install --dry-run`.
    - **CLI Output Reference**: Match the actual output from `./install --dry-run` (e.g., using 📦, ⚙️, 💾, 🔗, ✨ emojis and 2-space indentation for sub-steps).
    - **Configuration Highlights**: Explain `~/.gitconfig_shared`, `~/.vimrc_shared`, and `~/.config/fish/config_shared.fish`.
    - **Local Overrides**: 
      - Detail `~/.config/fish/config.local.fish` usage.
      - Explicitly document Node.js path handling using `fish_add_path` (e.g., `# fish_add_path /home/andrew/.nvm/versions/node/v20.11.1/bin`).
      - Explain that local settings can be added to the main config files (e.g., `~/.gitconfig`) alongside the shared include/source line.
    - **Maintenance**: How to run tests using `./tests/verify.sh --test-all`.
  </action>
  <verify>
    <automated>grep -q "sudo apt install" README.md && grep -q "config.local.fish" README.md && grep -q "fish_add_path" README.md</automated>
  </verify>
  <done>README.md is created with all required sections and accurate CLI examples.</done>
</task>

<task type="auto">
  <name>Task 2: Verify README content accuracy</name>
  <files>README.md</files>
  <action>
    Cross-reference the CLI output in README.md with the actual output of `./install --help` and `./install --dry-run` one last time to ensure 100% fidelity in emojis and formatting.
    Update README if any discrepancy is found.
  </action>
  <verify>
    <automated>grep -q "✨ Done!" README.md && grep -q "📦 Applying module" README.md</automated>
  </verify>
  <done>README accurately reflects CLI behavior and output style.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Documentation → User Shell | User manually executes commands from README. No direct code trust, but instructions must be safe. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | README.md | mitigate | Use clear, standard `apt` commands and explain what the `./install` script does before running. |
| T-04-02 | Information Disclosure | Local Overrides | mitigate | Emphasize that `*.local` files are for secrets/machine-specific data and are gitignored. |
</threat_model>

<verification>
Check README.md for:
1. All apt packages listed in research.
2. Correct emoji usage in examples.
3. Node.js path example in Overrides.
4. Test command accuracy.
</verification>

<success_criteria>
1. User can follow README to install all tools and run the sync script.
2. README is 100% accurate regarding the current script's output and behavior.
</success_criteria>

<output>
After completion, create .planning/phases/04-documentation/04-01-SUMMARY.md
</output>
